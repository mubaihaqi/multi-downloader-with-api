---
import "../styles/global.css";
import MainLayout from "../layouts/main";
import YoutubeLayout from "../layouts/ytmain";
import InstagramLayout from "../layouts/igmain";
import TiktokLayout from "../layouts/ttmain";
import TwitterLayout from "../layouts/twmain";
import SpotifyLayout from "../layouts/spmain";
import PinterestLayout from "../layouts/pinmain";
import ThreadsLayout from "../layouts/thdmain";
import XiaohongshuLayout from "../layouts/xhzmain";
import Navbar from "./navbar";
---

<div
  id="container"
  class="flex flex-col h-auto w-screen justify-start relative z-10"
>
  <Navbar client:load />
  <MainLayout client:load />
  <InstagramLayout client:load />
  <TiktokLayout client:load />
  <PinterestLayout client:load />
  <TwitterLayout client:load />
  <SpotifyLayout client:load />
  <YoutubeLayout client:load />

  <div class="relative w-full h-full mt-4 mb-6">
    <div
      class="absolute inset-0 flex items-center justify-center z-20 pointer-events-none"
    >
      <div
        class="bg-base-100/70 backdrop-blur-md p-6 py-8 rounded-2xl shadow-2xl border-2 border-base-content/10 max-w-lg w-full mx-4 text-center relative overflow-hidden modal-animate"
      >
        <!-- Decorative elements -->
        <div
          class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-primary via-secondary to-accent gradient-animate"
        >
        </div>
        <div
          class="absolute -top-4 -right-4 w-8 h-8 bg-primary/20 rounded-full float-animate"
        >
        </div>
        <div
          class="absolute -bottom-4 -left-4 w-6 h-6 bg-secondary/20 rounded-full float-animate"
          style="animation-delay: 1s;"
        >
        </div>

        <!-- Icon -->
        <div class="mb-4">
          <div
            class="w-16 h-16 bg-gradient-to-br from-primary to-secondary rounded-full flex items-center justify-center mx-auto mb-4 hover-lift"
          >
            <svg
              class="w-8 h-8 text-white"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"
              ></path>
            </svg>
          </div>
        </div>

        <!-- Content -->
        <h3 class="text-xl font-bold text-base-content mb-3">
          🚧 Fitur Sedang Dalam Pengembangan
        </h3>
        <p class="text-base-content/80 mb-4 leading-relaxed">
          Platform <strong>Threads</strong> dan <strong>Xiaohongshu</strong> sedang
          kami kembangkan dengan sepenuh hati untuk memberikan pengalaman terbaik.
        </p>

        <!-- Progress indicator -->
        <div class="w-full bg-base-300 rounded-full h-2 mb-4">
          <div
            class="bg-gradient-to-r from-primary to-secondary h-2 rounded-full w-3/4 pulse-animate"
          >
          </div>
        </div>

        <p class="text-sm text-base-content/60 font-medium">
          ⏳ Harap sabar menunggu ya! Kami akan segera meluncurkannya...
        </p>

        <!-- Coming soon badge -->
        <div class="mt-4">
          <span class="badge badge-primary badge-lg font-semibold hover-lift">
            Coming Soon
          </span>
        </div>

        <!-- Additional features -->
        <div class="mt-6 pt-4 border-t border-base-content/10">
          <div class="flex flex-wrap gap-2 justify-center">
            <span class="badge badge-outline badge-sm">🎬 Video Download</span>
            <span class="badge badge-outline badge-sm">🖼️ Image Download</span>
            <span class="badge badge-outline badge-sm">📱 Mobile Friendly</span>
            <span class="badge badge-outline badge-sm">⚡ Fast Processing</span>
          </div>
        </div>
      </div>
    </div>
    <div
      class="flex flex-col w-full h-full blur-xs pointer-events-none relative z-10"
    >
      <ThreadsLayout client:load />
      <XiaohongshuLayout client:load />
    </div>
  </div>
</div>
